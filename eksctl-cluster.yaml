apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: inventalert-cluster
  region: eu-west-1      # Ireland — closest full-service AWS region to Nigeria
  version: "1.32"

iam:
  withOIDC: true          # required for the EBS CSI driver addon IAM role

addons:
  - name: aws-ebs-csi-driver
    version: latest
    attachPolicyARNs:
      - arn:aws:iam::aws:policy/service-role/AmazonEBSCSIDriverPolicy

managedNodeGroups:
  - name: worker-nodes
    instanceType: t3.medium   # 2 vCPU / 4 GB — 2 nodes = 8 GB total, fits in 5 vCPU quota
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    volumeSize: 50            # GB, root EBS volume for container images and OS
    volumeType: gp3
    iam:
      attachPolicyARNs:
        - arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
        - arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
        - arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
